nav.links-grid
  a.link-card(href='{link.href}', target='_blank', rel='noopener noreferrer', title='{link.title}', @repeat='links as link')
    .icon
      i(className='{`${link.iconType} ${link.iconCode}`}')
    span {link.title}
